  'use strict';

  function gotKey (event) {
      
      var key = event.key;
      
      // Do something based on key press
	  // key to change time of day
	  // key to add models to stage?
	  // key to go into free camera
	  // key(s) to go into fixed camera positions
      
      // create a new shape and do a redo a draw
      draw();
  }
  
